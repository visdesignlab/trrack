import { Meta } from '@storybook/react';
import React, { FC } from 'react';
import _DemoComponent from './Trrack';

const meta: Meta = {
  title: 'Trrack-Vis',
};

export default meta;

export const DemoComponent: FC = () => <_DemoComponent />;
