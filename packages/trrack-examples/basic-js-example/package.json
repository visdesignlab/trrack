{
  "name": "basic-js-example",
  "private": true,
  "version": "1.0.1-alpha.3",
  "main": "index.js",
  "author": "Kiran Gadhave",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist && mkdir -p ../dist-basic-js && cp -r ./* ../dist-basic-js && mv ../dist-basic-js ./dist && rm -rf ./dist/package.json"
  }
}
