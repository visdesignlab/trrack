{
  "name": "basic-vis-example",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "author": "Kiran Gadhave",
  "license": "MIT",
  "scripts": {
      "build": "rm -rf ./dist && mkdir -p ../dist-basic-vis && cp -r ./* ../dist-basic-vis && mv ../dist-basic-vis ./dist && rm -rf ./dist/package.json"
  }
}
